# Category heatmap visual verification

- Desktop preview: heatmap is visible below the hero, shows category rows, five performance horizons, median returns, and observation counts.
- Mobile preview: the explanatory heading and heatmap container remain readable; the tabular region uses horizontal overflow rather than clipping the columns.
- No fabricated values were added. Cells with unavailable horizon observations display an em dash and zero/available observation counts.
- The homepage now uses the heatmap as the analytical entry point; the old explanatory journey cards are no longer rendered.
