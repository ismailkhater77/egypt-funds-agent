# Homepage heatmap reorder verification

The homepage now begins with the category performance heatmap immediately below the top bar. The hero panel no longer precedes the map in the first-screen flow. The map remains readable on desktop, with category labels on the right, horizon columns, median return cells, and observation counts. The legacy explanatory journey-card component is not rendered in the homepage flow.
